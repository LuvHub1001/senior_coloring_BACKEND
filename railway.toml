[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate deploy || echo 'Migration skipped (already applied)' && node src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
